---
apiVersion: v1
kind: Service
metadata:
  name: tdarr-server
  labels:
    app: tdarr-server
    app.kubernetes.io/name: tdarr-server
    app.kubernetes.io/component: server
spec:
  selector:
    app: tdarr-server
  ports:
    - name: web
      port: 8265
      targetPort: web
    - name: server
      port: 8266
      targetPort: server

---
apiVersion: v1
kind: Service
metadata:
  name: tdarr-server-external
  labels:
    app: tdarr-server-external
    app.kubernetes.io/name: tdarr-server-external
    app.kubernetes.io/component: server
    app.kubernetes.io/part-of: tdarr
spec:
  selector:
    app: tdarr-server
  type: NodePort
  ports:
    - name: server
      port: 8266
      targetPort: server
      nodePort: 30001

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tdarr-server
  labels:
    app: tdarr-server
    app.kubernetes.io/name: tdarr-server
    app.kubernetes.io/component: server
    app.kubernetes.io/part-of: tdarr
spec:
  replicas: 1
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app: tdarr-server
  template:
    metadata:
      labels:
        app: tdarr-server
        app.kubernetes.io/name: tdarr-server
        app.kubernetes.io/component: server
        app.kubernetes.io/part-of: tdarr
    spec:
      containers:
        - name: tdarr-server
          image: ghcr.io/haveagitgat/tdarr:latest
          imagePullPolicy: Always
          resources:
            limits:
              memory: "1Gi"
              cpu: "1"
          ports:
            - containerPort: 8265
              name: web
            - containerPort: 8266
              name: server
          # TODO:
          # https://tdarr.readme.io/reference/get_status
          # readinessProbe:
          #   initialDelaySeconds: 20
          #   periodSeconds: 10
          #   timeoutSeconds: 1
          #   successThreshold: 1
          #   failureThreshold: 6
          #   httpGet:
          #     path: /api/v2/status
          #     port: web
          # liveness
          envFrom:
            - configMapRef:
                name: tdarr-server-env
          volumeMounts:
            - name: nfs-server
              mountPath: /app/server
            - name: nfs-config
              mountPath: /app/configs
            - name: logs
              mountPath: /app/logs
            - name: nfs-raw-media
              mountPath: /media
            - name: nfs-cache
              mountPath: /temp
            - name: nfs-plex-media-movies
              mountPath: /opt/plex/libraries/movies
            - name: nfs-plex-media-uhd-movies
              mountPath: /opt/plex/libraries/uhd-movies
            - name: nfs-plex-media-tv
              mountPath: /opt/plex/libraries/tv-shows
      restartPolicy: Always

      volumes:
        - name: nfs-cache
          persistentVolumeClaim:
            claimName: tdarr-cache
        - name: logs
          emptyDir: {}
        # - name: server-config-json
        #   configMap:
        #     name: tdarr-server-config
        #     # set the file mode
        #     defaultMode: 0664
        #     items:
        #       - key: Tdarr_Server_Config.json
        #         path: Tdarr_Server_Config.json
        - name: nfs-raw-media
          persistentVolumeClaim:
            claimName: tdarr-arm-raw-pvc
        - name: nfs-server
          persistentVolumeClaim:
            claimName: tdarr-server
        - name: nfs-config
          persistentVolumeClaim:
            claimName: tdarr-config
        - name: nfs-plex-media-movies
          persistentVolumeClaim:
            claimName: plex-media-movies
        - name: nfs-plex-media-uhd-movies
          persistentVolumeClaim:
            claimName: plex-media-uhd-movies
        - name: nfs-plex-media-tv
          persistentVolumeClaim:
            claimName: plex-media-tv

      # Any filesystem mounted to the pod will be owned by this group
      securityContext:
        fsGroup: 1400

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: tdarr-server-env
  labels:
    app: tdarr-server
    app.kubernetes.io/name: tdarr-server-env
    app.kubernetes.io/component: config
    app.kubernetes.io/part-of: tdarr
data:
  TZ: "America/Denver"
  PUID: "1400"
  PGID: "1400"
  serverIP: "0.0.0.0"
  serverPort: "8266"
  webUIPort: "8265"
  internalNode: "false"
  inContainer: "true"
  ffmpegVersion: "6"
  nodeName: "K8sServer"
