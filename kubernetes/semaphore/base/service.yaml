---
apiVersion: v1
kind: Service
metadata:
  name: semaphore
  labels:
    app.kubernetes.io/name: semaphore
spec:
  selector:
    app: semaphore
  ports:
  - name: web
    port: 3000
    protocol: TCP
    targetPort: web
