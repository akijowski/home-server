---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: traefik-dashboard-custom-tls
  labels:
    app.kubernetes.io/name: traefik-dashboard-custom-tls
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`traefik.k8s.kijowski.casa`) && (PathPrefix(`/dashboard`) || PathPrefix(`/api`))
      kind: Rule
      services:
        - name: api@internal
          kind: TraefikService
  tls:
    certResolver: aws-dns
