#!/usr/bin/env ansible-playbook

# ./showgroups -i ./inventory -l molecule-consul-0
- hosts: all
  gather_facts: no
  tasks:
  - name: show the groups the host(s) are in
    debug:
      msg: "{{group_names}}"
