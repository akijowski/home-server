version: 3

tasks:
  default: task --list

# https://stackoverflow.com/questions/46184125/how-to-merge-kubectl-config-file-with-kube-config
  config:
    desc: merge k3s cluster configs to a single config
    aliases:
      - merge
    cmds:
      - ./bin/merge_k8s_config
